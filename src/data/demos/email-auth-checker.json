{
  "toolId": "email-auth-checker",
  "chapter": "ch-37-email-header-authentication-checker",
  "route": "/tools/email-auth-checker",
  "title": "Email Header Authentication Checker",
  "blurb": "What the headers claim, checked against what DNS actually says.",
  "steps": [
    {
      "say": "Every email carries a block of headers most people never look at. Inside them is a claim about whether the sender was authenticated. This tool reads that claim, then goes and checks it independently against live DNS.",
      "settle": 700
    },
    {
      "say": "Loading a sample header block, so nothing private goes on screen.",
      "at": "eauth-sample",
      "act": "click",
      "settle": 1200
    },
    {
      "say": "This is what real headers look like. The from address says GitHub. There is an authentication results line where the receiving mail server recorded its own verdict, and a DKIM signature naming a domain and a selector.",
      "at": "eauth-input",
      "settle": 2400
    },
    {
      "say": "Checking. This makes real DNS queries, so it takes a moment.",
      "at": "eauth-check",
      "act": "click",
      "waitFor": "eauth-verdict",
      "settle": 1600,
      "expect": "Independent live DNS checks"
    },
    {
      "say": "Likely legitimate, and the reason is stated rather than implied: the domain enforces a DMARC policy and the alignment checks pass.",
      "at": "eauth-verdict",
      "settle": 2000
    },
    {
      "say": "Here is the independent half. SPF says which servers may send for this domain, and GitHub's record ends in a soft fail, meaning suspicious mail is marked rather than rejected outright. DMARC is set to quarantine, with subdomains set to reject. The DKIM key named in the signature is live in DNS and has not been revoked.",
      "at": "eauth-dns",
      "settle": 3000,
      "expect": "sp=reject"
    },
    {
      "say": "Alignment is the part that catches phishing. A message can pass SPF and DKIM honestly and still be a fake, if the domain it passed for is not the domain in the from address. Both align here. When they do not, that is the signal worth acting on.",
      "at": "eauth-alignment",
      "settle": 2600
    },
    {
      "say": "And the limits, stated plainly. It does not cryptographically verify the DKIM signature, because that needs the full message body. DNS shows the domain's configuration now, not when the mail was sent. And the receiving server's verdict is only as trustworthy as the server that stamped it. This tool relays that claim, it does not re-check it.",
      "at": "eauth-caveats",
      "settle": 2800
    }
  ]
}
