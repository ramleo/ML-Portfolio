Sample file for the YARA File Scanner walkthrough.

Nothing here is malicious. It exists so the tool's custom-rule mode has
something to actually match: the example rule shipped in the editor looks
for a marker string, and this file contains one.

    SECRET_MARKER_XYZ

Write your own rule, point it at your own file, and the scanner tells you
where it matched and what bytes it saw.
